import { redirect } from "next/navigation";
import MainPage from "./components/MainPage";

const Page = () => {

  return (
    <section>
      <MainPage />
    </section>
  );
};

export default Page;
